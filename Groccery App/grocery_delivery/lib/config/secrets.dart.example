/// Secrets configuration template
/// Copy this file to secrets.dart and fill in your actual values
/// NEVER commit secrets.dart to git!

class Secrets {
  // Firebase Cloud Messaging
  static const String fcmServerKey = 'YOUR_FCM_SERVER_KEY';

  // Notifications API (should be backend endpoint)
  static const String notificationsApi = 'https://your-backend-url.com/api/v1/notifications';

  // CRITICAL: Payment gateway keys should NEVER be in client-side code
  // All payment verification must be done on secure backend
  // This is only for backend service reference
  static const String paymentApiUrl = 'https://your-backend-url.com/api/v1/payments/verify';

  // Yoco Payment Gateway (backend only)
  // WARNING: NEVER put secret keys in client code
  // This should only be used in secure backend services
  static const String yocoPublicKey = 'YOUR_YOCO_PUBLIC_KEY';

  // Sentry/Error Reporting DSN
  static const String sentryDsn = 'YOUR_SENTRY_DSN';

  // Analytics keys
  static const String analyticsKey = 'YOUR_ANALYTICS_KEY';

  // Google Maps API Key (if using)
  static const String googleMapsApiKey = 'YOUR_GOOGLE_MAPS_API_KEY';
}
